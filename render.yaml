services:
  - type: web
    name: pixishift-backend
    runtime: python
    buildCommand: apt-get install -y libreoffice && pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT